// import probe from "probe-image-size";

// export const isKpoppingBlocker = async (imageUrl) => {
//     const imageMetadata = await probe(imageUrl);
//     const { width, height } = imageMetadata;
//     return (width === 1200 && height === 630);
// }